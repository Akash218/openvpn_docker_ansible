var=$(netstat -ntpl | cut -d":" -f2 | cut -d" " -f1 | sed '/^[A-Z|a-z]/d')
j=1024
if [ "${j}" -le 65535 ]; then
  for i in "${var}"; do
  if [ "$i" = "$j" ]; then
    (( j++ ))
  else
    port="${j}"
  fi
done
else
  echo " no more ports are available "
fi
echo $port
